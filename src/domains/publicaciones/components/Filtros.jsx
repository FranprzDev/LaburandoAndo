import FormFiltros from './FormFiltros';

const Filtros = () => {
    return (
        <div className='h-100 d-flex w-100'>
            <FormFiltros/>
        </div>
    );
};

export default Filtros;
